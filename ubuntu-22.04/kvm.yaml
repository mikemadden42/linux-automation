# kvm.yaml
actions:
  - action: package.install
    provider: apt
    list:
      - bridge-utils
      - cpu-checker
      - libvirt-clients
      - libvirt-daemon-system
      - qemu-kvm
      - virt-manager
      - virtinst
